# Weekly Earnings Options Research

Run `python -m earnings_export analyze-next-week-options` from the project root.

Read the generated Markdown and JSON artifacts for this run. Summarize only:

- eligible candidates;
- data limitations; and
- research rationale.

If the candidate list is empty, state: "No eligible candidate was found."

This is research only. Do not submit or simulate an order. Do not make any changes
outside the generated research artifacts.
